/*
Diferencia entre estructuras de datos Fisicas y Logicas:

-> Fisicas:
    Pueden ser los array o las listas ligadas, y guardan informacion

-> Logicas:
    Son las reglas por las cuales se definen como se introducen los valores en las estructuras físicas
    Pueden ser:
        Stack(Pila) -> Lineal -> El ultimo que entra, el primero que sale
        Queues(Colas) -> Lineal -> El primero que entra el primero que sale
        Trees(Arboles) -> No lineal -> Organizada por Jerarquia, conexion de nodos organizada
        Graph(Grafos) -> No lineal -> Conexion de nodos 
        Hash Table(Tablas Hash) -> Tabular

*/